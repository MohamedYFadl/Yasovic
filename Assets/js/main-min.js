$(document).ready(function () { $(".loader").fadeOut(500, function () { $(".loading").fadeOut(500, function () { $("body").removeClass("overflow-hidden"), $(".loading").remove() }) }); let e = $("#about").offset().top, t = $("nav").outerHeight(), n = document.querySelectorAll(".about .progress-bar"); $(window).on("scroll", function () { $(window).scrollTop() > e - t ? ($("nav").addClass("nav-scroll"), n.forEach(e => { e.style.width = e.dataset.width })) : $("nav").removeClass("nav-scroll") }) })(function () { emailjs.init("SUU_me7mCjuPy_ts_") })(); const form = document.getElementById("contact-form"), status = document.getElementById("status"); form.addEventListener("submit", function (e) { e.preventDefault(), status.innerText = "Sending Email ...", emailjs.sendForm("service_1e2ockb", "template_2mnk10b", this).then(() => { status.innerText = "The message has been sent", form.reset() }, () => { status.innerText = "Error while sending the message, please try again" }) });